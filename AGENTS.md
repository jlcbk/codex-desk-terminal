# 开发 Agent 协作约定

项目当前状态：**双源（Codex 受控会话 / ZCode 桌面观察）v1.0 定稿**——六屏 UI、Wi-Fi/WSS 整机、低压保护、像素回归体系均已实现并验证；未完成项（BLE 真机、功耗实测、桌面旁听）以 `docs/STATUS.md` 标注为准。不要把文档中的计划性表述当作未实现的判断依据，以 STATUS 台账为准。

## 真源和认领

- 需求、任务编号、依赖与验收真源：`docs/DEVELOPMENT_PLAN.md`。
- State 与模块边界真源：`docs/INTERFACES.md`（v1 冻结 + v1.1/v1.2 可选增补；变更走 A0 裁决并记录）。
- 实施状态唯一台账：`docs/STATUS.md`，每个任务记录 owner、状态、依赖、提交、验证命令、证据路径和阻塞项。不要重复维护另一份 backlog。
- 单个任务只有一个 owner；共享协议、顶层构建、版本锁和 golden 更新由 A0 统一合并。其他 Agent 提出具体变更，不自行绕开协议。
- 可用独立 Git worktree 并行；共享一块板时预约测试窗口，不并发烧录或改电源。

## 实现边界

- UI 不调用 ESP-IDF、网络、ADC 或文件 IO；硬件驱动不解释 Codex 事件；Transport 不生成业务状态。
- LVGL 在 PC 和设备均锁定 9.3.0。不要独立升级任一端或复制两份 UI。
- 设备本地电池保护优先于页面、无线和远端数据。生产构建禁用远端电池注入。
- ACK 只表示本地静音/已读，绝不等于批准 Codex 操作。凭证不进入 State、fixtures 或日志。
- 不用 SDL 结果证明硬件睡眠、无线电流或 LCD 保持效果；没有实测，状态标记为未验证。
- 不添加产品电流传感器。功耗对比用外部仪器；不得显示编造的电流或精确 SOC。

## 每个任务的完成交付

提供：修改路径、任务编号、接口变化、可复现命令、通过/失败结果、证据路径、剩余问题。非平凡逻辑留下最小可运行检查；不可把测试截图自动覆盖为 golden 来消除失败。

先完成自己的依赖和边界内任务。上游受阻时用已冻结 fixtures/stub 继续独立工作，但不将 Mock 验收标记成真实接入验收。
